<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8">
  <title>Omdirigerar...</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <script>
    (function () {
      function isIOS() {
        // Täcker iPhone, iPad, iPod samt iPadOS 13+ som ibland rapporterar "Mac"
        return /iPad|iPhone|iPod/.test(navigator.userAgent) ||
               (navigator.platform === 'MacIntel' && navigator.maxTouchPoints > 1);
      }

      function isAndroid() {
        return /android/i.test(navigator.userAgent);
      }

      // Dina faktiska länkar (korrigerade)
      const iosLink = "https://apps.apple.com/se/app/garantera/id6733228143";
      const androidLink = "https://play.google.com/store/apps/details?id=com.ebeco.ebecoconnect";
      const desktopLink = "https://www.ebeco.se";

      function redirect() {
        try {
          if (isIOS()) {
            window.location.replace(iosLink);
          } else if (isAndroid()) {
            window.location.replace(androidLink);
          } else {
            window.location.replace(desktopLink);
          }
        } catch (e) {
          // Om något går snett – gå till desktop som sista utväg
          window.location.href = desktopLink;
        }
      }

      // Kör så fort DOM är redo
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', redirect);
      } else {
        redirect();
      }
    })();
  </script>
  <noscript>
    <meta http-equiv="refresh" content="0; url=https://www.ebeco.se">
  </noscript>
</head>
<body>
  <p>Omdirigerar dig till rätt app...</p>
  <p style="margin-top:1rem;font-size:0.9rem;">
    Om inget händer, klicka:
    https://apps.apple.com/se/app/garantera/id6733228143iOS</a> ·
    https://play.google.com/store/apps/details?id=com.ebeco.ebecoconnect&hl=sv&gl=SEAndroid</a> ·
    https://www.ebeco.seDesktop</a>
  </p>
</body>
</html>
